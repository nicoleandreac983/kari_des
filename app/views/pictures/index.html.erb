<p style="color: green"><%= notice %></p>

<h1>Pictures</h1>

<div id="pictures">
  <% @pictures.each do |picture| %>
    <%= render picture %>

      <div class="form-group">
    <%= form.label :name, "Nombre de la imagen" %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :description, "Descripción de la imagen" %>
    <%= form.text_area :description, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :url, "Agrega tu imagen" %>
    <%= form.text_field :url, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :context, "Describe donde estabas" %>
    <%= form.text_field :context, class: 'form-control' %>
  </div>
    <p>
      <%= link_to "Show this picture", picture %>
    </p>
  <% end %>
</div>

<%= link_to "New picture", new_picture_path %>
